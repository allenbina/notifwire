// notifwire is a static, client-rendered desktop app: prerender to plain files
// and disable SSR so the whole UI ships as a Tauri-served bundle.
export const prerender = true;
export const ssr = false;
